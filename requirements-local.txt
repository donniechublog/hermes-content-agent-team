# Phu thuoc CHI CAN TREN MAY LOCAL xu ly anh nang (doi_chu_anh.py / Gin).
#
# KHONG dua vao requirements.txt: file do phuc vu venv CHUNG voi hermes tren
# server (xem dau file), va torch + simple-lama-inpainting nang ~2GB — chinh
# thu server khong cong noi, ly do venv rieng cho may nay ton tai tu dau.
#
# Cai (venv rieng, KHONG phai venv/hermes-agent tren server):
#     python3 -m venv venv
#     venv/bin/pip install -r requirements.txt -r requirements-local.txt

opencv-python-headless   # doc/ghi anh, dung mask, dilate
easyocr                  # dinh vi vung chu (khong can doc dung noi dung)
torch                    # nen cua LaMa; tren macOS pip tu chon ban CPU/MPS
simple-lama-inpainting   # inpaint LaMa — cv2.inpaint de lai vet loang tren anh phuc tap
